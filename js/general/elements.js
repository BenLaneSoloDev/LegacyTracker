const personElm = document.getElementById("person-select");             // Person Input Field
const suggestionElm = document.getElementById("person-list");           // Person Drop Down
//const roleElm = document.querySelector(".role");                      // Role Div
const formElm = document.querySelector(".filter-form");                 // Form Element
const gridElm = document.querySelector(".grid");                        // Grid Div

export { personElm, suggestionElm, formElm, gridElm }